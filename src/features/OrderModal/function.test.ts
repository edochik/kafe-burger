export { }
import { validationValues } from "./validationValues"
import { changeReceiving } from "./changeReceiving"

describe('тестирование функций компонента OrderModal', () => {
	test('функция validationValues получает Number и возвращает объект со значением', () => {
		// const result = validationValues()
	});
	test('validationValues', () => {

	});
	test('validationValues', () => {

	});
	test('validationValues', () => {

	});
	test('changeReceiving', () => {

	});
})